# W4-A Checkpoint: world_critic_service 分级（4 档）

**Date**: 2026-05-10
**Task**: W4 Task 4.1 / 4.2 / 4.3 — world critic 分级

## Status: DONE

### Files modified

- **Created** `backend/services/world_critic_service.py`
- **Created** `backend/tests/test_world_critic_shape.py`
- **Created** `backend/tests/test_world_critic_light.py`

### Tests

```
16 passed in 1.02s
- test_world_critic_shape.py: 10 tests (all PASS)
- test_world_critic_light.py: 6 tests (all PASS)
```

Full regression: **536 passed**, 2 deselected, 3 pre-existing errors (unrelated to this task).

### Implementation summary

**`validate_world_shape(payload: dict) -> list[str]`** — pure Python, no LLM:
- playable[].name ⊆ world_characters[].name
- world_characters[].schedule.values() ⊆ locations[].name
- world_characters[].initial_location ⊆ locations[].name
- shared_events[].involved_npcs ⊆ world_characters[].name
- events_data npc_intent_driven trigger.npc_name ⊆ world_characters[].name
- events_data rumors[].knower_npcs ⊆ world_characters[].name
- events_data effects.npc_mood_changes keys ⊆ world_characters[].name
- disabled events skipped (no double-warn)

**`async light_critic_lore(lore_pack, ip_canon, llm_router) -> list[str]`** — single LLM pass:
- Empty dimensions → return [] without calling LLM
- LLM failure / JSON parse failure → return [] (non-blocking)

**`async light_critic_shared_events(shared_events, ip_canon, llm_router) -> list[str]`** — single LLM pass:
- Empty list → return [] without calling LLM
- LLM failure / JSON parse failure → return [] (non-blocking)

**`async heavy_critic_characters_stub(...) -> tuple[dict, list[str]]`** — stub for W4-C M2-D:
- Returns (input payload, []) unchanged

Both light critics reuse `_collect_stream_text` and `_extract_json_from_text` from `services/research_pack_builder.py`.

### Concerns / Notes

None. All spec requirements met. Pre-existing 3 errors in regression are unrelated to this task (test_orchestrator_timing, test_prompts, test_world_progression).

# Checkpoint: Task 1.2 — Feature flag + 容量上限常量

## Status: DONE

### Files modified
- `/Users/jie/Desktop/code/pokonyan/talealive/backend/config.py` — Added 4 new configuration fields in Settings class
- `/Users/jie/Desktop/code/pokonyan/talealive/backend/tests/test_world_creator_v2_settings.py` — New test file (created)

### Fields added
```python
world_creator_v2_enabled: bool = False
research_pack_max_passages: int = 100
research_pack_max_passage_chars: int = 600
research_pack_max_admin_description_chars: int = 50_000
```

### Tests
**Command:**
```
/Users/jie/Desktop/code/pokonyan/talealive/.venv/bin/python -m pytest tests/test_world_creator_v2_settings.py -v
```

**Result:**
```
tests/test_world_creator_v2_settings.py::test_world_creator_v2_flag_default_false PASSED
tests/test_world_creator_v2_settings.py::test_research_pack_limits_present PASSED

2 passed in 0.01s ✓
```

**Regression check:**
- Core module imports verified (config, models, services) — all working
- Existing fields still accessible and working correctly
- Tests marked with `@pytest.mark.no_db` to avoid pre-existing SQLite JSONB compatibility issue unrelated to this task

### Notes
- All 4 fields follow existing `config.py` naming and type conventions
- Default values and limits chosen per spec requirements
- No impact on existing code; fields are net-new
- Task scope strictly contained to settings addition; no cross-module usage yet (per plan Step 3)

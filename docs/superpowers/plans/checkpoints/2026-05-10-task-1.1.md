# Task 1.1 Checkpoint — Alembic Migration + ORM Fields

Date: 2026-05-10

## Files Modified

- `backend/models/world.py` — added `lore_pack`, `shared_events`, `events_data` (JSONB nullable) to `World`; added `JSONB` import + `_JSONB` type alias with SQLite fallback via `JSON().with_variant(JSONB(), "postgresql")`
- `backend/models/generation_task.py` — added `intermediate_state` (JSONB nullable) to `GenerationTask`; same import pattern

## Files Created

- `backend/migrations/versions/531ec5f45068_world_creator_v2_fields.py` — manual migration (autogenerate not viable; dev DB is SQLite, not PostgreSQL)
- `backend/tests/test_world_creator_v2_migration.py` — 2 new tests

## Migration

- Revision hash: `531ec5f45068`
- Down revision: `f5a2b3c4d6e7` (add_related_npc_to_memory_entries, most recent prior migration)

## Test Results

- `test_world_creator_v2_migration.py`: 2 passed
- `test_admin_drafts_api.py`: 4 passed
- `test_generation_task_record_event.py`: 2 passed

## Caveats

- `JSONB` type is PostgreSQL-specific. For the SQLite test environment, a `_JSONB = JSON().with_variant(JSONB(), "postgresql")` alias is used in both model files so `create_all` works under SQLite while production PostgreSQL gets real JSONB.
- Migration was created manually (not autogenerated) because `alembic revision --autogenerate` requires the target DB to be at HEAD, and the dev environment uses SQLite which doesn't reflect the full migration history cleanly. The migration content was hand-written per the plan spec and matches the ORM fields exactly.

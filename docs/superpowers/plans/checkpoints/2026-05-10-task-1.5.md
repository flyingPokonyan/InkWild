# Task 1.5 Checkpoint — ResearchBroker.collect_passages

## Status: DONE

### Files modified

- `backend/services/research_broker.py` — added `collect_passages` method + imported `uuid` stdlib module and `Passage` from `schemas.research_pack`
- `backend/tests/test_research_broker_passages.py` — new test file (4 tests)

### Method signature added

```python
async def collect_passages(self, request: ResearchRequest, max_chars: int) -> list[Passage]:
```

Located between `collect_artifacts` and `summarize` in `ResearchBroker`.

### Tests

Command:
```
/Users/jie/Desktop/code/pokonyan/talealive/.venv/bin/python -m pytest tests/test_research_broker_passages.py tests/test_research_broker.py -v
```

Result: **9 passed** (5 existing + 4 new), 0 failed, 0.53s

### Concerns / Notes

- `ResearchRequest` is in `schemas.generation_strategy` (not `schemas.research`). Task spec's test template had `from schemas.research import ResearchRequest` which would fail — adapted to actual import path.
- Constructor third param is `synthesizer` (not `llm_router`). Tests updated accordingly.
- No `MAX_TAVILY_QUERIES_PER_REQUEST` constant exists; `_dedupe_queries` is called with `request.max_queries` (already on the model, default 3).
- `_dedupe_queries` reused exactly as-is from existing code.
- Single-query exception handling: `try/except continue` per-query; whole-request exception path returns empty list.

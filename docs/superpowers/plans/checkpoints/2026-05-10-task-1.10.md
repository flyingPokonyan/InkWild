# Task 1.10 Checkpoint — intermediate_state 写入

## Status: DONE

## Files modified

- `backend/services/generation_task_service.py` — added `record_intermediate` method
- `backend/services/world_creator_agent_v2.py` — extended `__init__` with `task_service` / `task_id`; added checkpoint call in `create_world` before emitting `research_pack completed`; removed duplicate `pack_dict` assignment
- `backend/tests/test_intermediate_state.py` — new test file (5 tests)

## record_intermediate signature

```python
async def record_intermediate(self, task_id: str, phase: str, snapshot: dict) -> None:
    """Snapshot phase 产物到 intermediate_state JSON 字段（merge，不覆盖其他 phase）。

    单事务 + with_for_update 防止并发竞态。失败 raise，调用方决定要不要 retry。
    """
```

Implementation: `select + with_for_update → dict(task.intermediate_state or {}) → current[phase] = snapshot → task.intermediate_state = current → commit`. `dict()` copy needed to trigger ORM dirty detection on JSON field.

## v2 agent injection path

`_run_world_generation(task_id)` → `WorldCreatorAgentV2(llm=..., image_gen=..., broker=..., task_service=self, task_id=task_id)`

In `create_world`: after `build_research_pack` succeeds (or degrades to empty pack), compute `pack_dict = pack.model_dump()`, then call `await self.task_service.record_intermediate(self.task_id, phase="research_pack", snapshot=pack_dict)` if both `task_service` and `task_id` are set. Failure is caught and logged as warning — does not block the generation flow.

## Tests

```
tests/test_intermediate_state.py::test_record_intermediate_creates_initial_state PASSED
tests/test_intermediate_state.py::test_record_intermediate_merges_multiple_phases PASSED
tests/test_intermediate_state.py::test_record_intermediate_overwrites_same_phase PASSED
tests/test_intermediate_state.py::test_record_intermediate_unknown_task_raises PASSED
tests/test_intermediate_state.py::test_v2_agent_records_research_pack_intermediate PASSED
tests/test_world_creator_v2_entry.py (6 tests) — all PASSED
Full suite (excluding 2 pre-existing failures): 381 passed
```

## Concerns / Notes

- 2 pre-existing test failures unrelated to this task:
  - `tests/test_event_system.py::test_already_triggered_skipped` — event already-triggered dedup logic bug
  - `tests/test_image_storage.py::test_get_image_storage_returns_oss_backend` — OSS key prefix mismatch
- `with_for_update()` degrades to no-op on SQLite (used in tests) — correct behavior, no concern
- `task_service` and `task_id` are optional (`| None`) to preserve backward compatibility with unit tests that construct `WorldCreatorAgentV2` without service injection

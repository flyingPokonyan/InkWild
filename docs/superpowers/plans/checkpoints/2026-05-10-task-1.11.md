# Task 1.11 Checkpoint — ResearchPackSection（read-only 草稿展示）

## Status: DONE_WITH_CONCERNS

---

### Files modified

| Action | File |
|--------|------|
| Create | `frontend/components/admin/editor/sections/ResearchPackSection.tsx` |
| Modify | `frontend/app/admin/worlds/drafts/[id]/page.tsx` |

Note: `DraftEditorShell.tsx` was **not** modified. The section is registered in the world draft page's `WorldEditorBody` component (which is the appropriate location per the shell's `renderBody` pattern). `DraftEditorShell` is a generic shell that delegates all section rendering to the `renderBody` render prop — it has no world-specific awareness and is not the right place to add world-specific sections.

### Registration location

**File:** `frontend/app/admin/worlds/drafts/[id]/page.tsx`

Changes:
1. Lines 8–9: Added imports for `ResearchPackSection` and `ResearchPackData`
2. In `WorldEditorBody` (approx. line 80–84): Added defensive `researchPack` extraction from payload via intersection type cast
3. First element inside the `<>` fragment return (before `section-basic` EditorSection): `<ResearchPackSection researchPack={researchPack} />`

The component renders `null` when `researchPack` is falsy, so it's invisible when `research_pack` is absent from payload (current default state). It will appear automatically once the backend includes `research_pack` in the normalized world payload.

---

### Data path adapter

**Current situation:** `research_pack` is NOT included in `draft.payload` because `_normalize_world_payload` in `backend/api/admin.py` (line 187–203) explicitly strips it. The v2 agent stores `research_pack` in `generation_task.intermediate_state`, but `_serialize_generation_task` (line 298–325) also does not include `intermediate_state` in the API response.

**result path:**
- `research_pack` comes via SSE `result` event payload: `{ research_pack: {...} }`
- After SSE `done`, `loadDraft()` reloads the draft detail — but at this point `research_pack` has been lost (stripped by normalizer)

**research_pack actual location in draft object:** currently nowhere accessible to the frontend after task completes.

**What needs to happen for this to work (backend follow-up required):**

Option A (preferred, minimal change): Update `_normalize_world_payload` to pass through `research_pack` when present:
```python
def _normalize_world_payload(payload: dict) -> dict:
    result = { ... }  # existing fields
    if "research_pack" in payload:
        result["research_pack"] = payload["research_pack"]
    return result
```
Then `draft.payload.research_pack` will be populated and the frontend will show it.

Option B: Include `intermediate_state` in `_serialize_generation_task` and thread it through to `renderBody` context. More invasive.

**Frontend component path:** `(payload as Payload & { research_pack?: ResearchPackData }).research_pack` — safe defensive cast, no runtime risk.

---

### TS / lint

- `npx tsc --noEmit`: **PASS** (0 errors)
- `npx eslint components/admin/editor/sections/ResearchPackSection.tsx`: **PASS** (0 errors, 0 warnings)
- `npx eslint "app/admin/worlds/drafts/[id]/page.tsx"`: **PASS** (0 errors, 0 warnings)
- Forbidden token check (`color-accent`, `font-size-`, `ta-`): **0 hits**
- Forbidden arbitrary sizing (`text-[Xrem]`): **0 hits**

---

### Visual compliance

- All font sizes use `.lv-t-*` classes: `lv-t-h2`, `lv-t-h3`, `lv-t-body`, `lv-t-meta`, `lv-t-caps`
- All colors use `var(--lv-*)` tokens: `--lv-line`, `--lv-line-2`, `--lv-bg-1`, `--lv-bg-2`, `--lv-ink`, `--lv-ink-2`, `--lv-ink-3`, `--lv-ink-4`, `--lv-accent`, `--lv-font-mono`
- No inline `style={{fontSize: number}}`
- No `text-[Xrem]` arbitrary Tailwind classes
- Touch target on expand button: `minHeight: 44` ✅

---

### Concerns / Notes

1. **Backend must update `_normalize_world_payload`** to pass through `research_pack` field for this component to actually render. Without that change, the section is permanently invisible (renders null). This is a backend follow-up for M1 completion.

2. **sections/ directory** did not exist — created as part of this task. This is the first component in the directory.

3. **`DraftEditorShell.tsx` not modified** (contrary to original task spec mentioning it). The shell's generic `renderBody` render-prop pattern means world-specific sections belong in the world draft page, not the shell. The registration is functionally equivalent.

4. The `source` field in `Passage` is typed as `"tavily" | "ip_probe" | "admin_note"`. The `SOURCE_LABELS` map has a fallback (`?? p.source`) for unknown sources added at runtime.

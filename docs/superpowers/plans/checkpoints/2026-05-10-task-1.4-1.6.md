## Status: DONE

### Files modified
- `backend/services/research_pack_builder.py` (new)
- `backend/tests/test_research_pack_builder.py` (new)

### LLMRouter API 选用
- **方法名**: `stream_with_tools(messages, tools, system, max_tokens)`
- **适配说明**: LLMRouter 只暴露 `stream_with_tools`，无 `complete_json` / `complete` / `dispatch`。
  `probe_ip_canon` 内部用 `tools=[]` 调用该方法触发纯文本流，逐 `text_delta` 事件拼接，
  再用 `_extract_json_from_text`（兼容 Markdown 代码块）解析 JSON。
  测试中的 fake router 实现为带 `async def stream_with_tools(self, **kwargs)` 的异步生成器类，
  无需 `AsyncMock`（避免 `AsyncMock` 无法直接用作 `async for` 的兼容性问题）。

### Tests
- 命令: `/Users/jie/Desktop/code/pokonyan/talealive/.venv/bin/python -m pytest tests/test_research_pack_builder.py -v --no-header`
- 结果: **8 passed**

### 回归
- 命令: `pytest tests/test_research_pack_schema.py tests/test_world_creator_v2_settings.py tests/test_research_pack_builder.py -v`
- 结果: **18 passed**

### Concerns / Notes
- 计划模板假设 `complete_json(slot, system, messages, max_tokens)` 存在，实际项目没有此方法。
  已按真实 API 适配，测试 mock 也同步改为 `stream_with_tools` 风格的异步生成器类。
- `_ErrorRouter.stream_with_tools` 需要在 `raise` 前有 `yield` 才能成为合法的异步生成器；
  测试用例 `test_probe_ip_canon_handles_llm_exception` 已验证异常被捕获、返回空 IPCanon。
- Task 1.7 的 `build_research_pack`（三路并发合并）预留为 TODO，本文件 docstring 已注明。
